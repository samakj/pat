/** @format */

import React from 'react';
import { HeaderElement, PageHeaderElement } from './elements';

export const Header: React.FunctionComponent = () => {
  return (
    <PageHeaderElement>
      <HeaderElement>Pat</HeaderElement>
    </PageHeaderElement>
  );
};
